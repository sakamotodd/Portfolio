import React from 'react';
import { Layout } from '../components/common/Layout';
import { LoginAuth } from '../components/loginPage';

const Login = () => {
  return <LoginAuth />;
};

export default Login;
